apiVersion: v1
kind: Service
metadata:
  name: godzilla-api
  labels:
    app: godzilla-api
spec:
  type: LoadBalancer
  selector:
    app: godzilla-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
